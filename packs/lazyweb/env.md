# Lazyweb Environment

## Required local environment
- `LAZYWEB_MCP_TOKEN` (local only, never committed)

## Repository policy
- `.env` and token files are ignored.
- `.lazyweb/` local output must remain untracked.
